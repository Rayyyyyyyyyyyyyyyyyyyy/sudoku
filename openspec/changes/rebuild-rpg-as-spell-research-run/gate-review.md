# 實作閘門審查（2026-09-16）

HEAD `48d3178`（父提交 `a8bc804`）。本輪已授權實作；目前交付獨立可玩實驗，正式遊戲仍為 v2。

## 已確認並落入規格的決定

- 戰前免費重配；配装失敗不代表資產沒有可勝解。
- 預覽長咒後改選短咒仍保留已揭示意圖；預覽不耗回合、不重抽。
- 長咒在最後詠唱回合的敵方行動後立即完成，死亡或有效打斷則不完成。
- 使用者選定章節基線通關保證：包含撤離後的新基線，須存在不靠撤離的通關策略；M1–M3 檢查該策略在所有敵方意圖分支下帶入的真實資源。失誤耗損入口不保證三種獲勝開局，可以回章節重配。

## 已完成的切片證據

五成分各兩張殘頁、空鎧騎士／鐵脊龍鱷、緘默／迴響，提供純 TS evaluator、狀態機、資訊搜尋、CLI 與 React 實驗頁。
具體數值、效果期限、結算顺序、搜尋預算與重現指令見 [slice-contract.md](slice-contract.md)，機器結果見 [slice-evidence.json](slice-evidence.json)。

- 基線騎士最壞剩 26/40 HP；其策略產生三個不同龍鱷入口，均保留實際 4 MP，最壞通關剩 10/40 HP。
- 每個策略入口至少四種已證明獲勝開局；一個額外開局 `UNKNOWN`，未計入成功。
- M3 騎士 0.723、三個龍鱷入口均 0.333；撤離提交的新基線亦通過。
- seed 0 實際產生 37 HP／0 MP 的偏離策略入口，確認原先要求每個任意可達入口至少三手不成立。
- seed 0 配裝對照、決策 11/11、九種固定策略無法完成此切片；不推廣為五章／空與全解鎖 profile 的結論。
- 新增 32 項測試通過；既有應用 205 項、typecheck、build 通過。瀏覽器操作完成兩戰與殘頁鍵盤移動，360px 無橫向溢出。正式離線流程未重驗。

0.2 的最小實驗已完成；0.1 整體規格閘門仍未關閉。正式 v3 任務不可用實驗代勾，也尚不擴展至 60 張殘頁。

## 正式實作前仍須關閉的缺口

| 項目 | 剩餘工作 |
| --- | --- |
| 全文語意同步 | 把切片已凍結 evaluator 契約整合進正式規格，覆核死亡／打斷／中立排序／反傷的所有跨檔描述。 |
| 有效池／容量 | 列出完整各解鎖階段法術數、持有上限與有效池；目前只驗證實驗 3×5 格／10 張。 |
| 一次性給予 | 正式供給表須區分唯一給予 ID 數與 amount 總和（首次通關為 2）；明定組成獎勵與戰勝獎勵的領取時機。 |
| profile 與同成分 | 「完整保留」須容許追加歷史；「每行新行為」須區分獨立效果層與不同效果種類。 |
| 認證一般化 | 實驗 M3 以終端路徑計數，並非隨機敵方行動的機率勝率；正式規格須固定策略集合、分母與決策投影。 |
| 生產驗證 | 尚無五章生成器、S1–S3 篩檢、空／全解鎖 profile 全章驗證、安全模板、unknown-input 存檔驗證或遷移。 |
| 外部依據 | task 1.4 的一手来源覆核未完成；暫記不作玩法或結案證據。 |

## 已完成的前置修正

重跑 v2 原始命令並保存 `baseline-2026-09-16.jsonl`；撤回 v3 三小時與六人門檻；修正中立格位整理、S1 等回合誤拒、S2 將低傷害當零 DPS、禁配已被證明等過度宣稱。tasks 1.1–1.3 保持完成。

## v2 產物退役清單（task 1.3）

| 現有產物 | 後續處理及相容性責任 |
| --- | --- |
| `src/data/rpg/story.ts` 的 64 節故事圖、paragraphs／variants | v3 改用章節圖與規則文字；先凍結 v1／v2 辨識資料再換 runtime catalog。 |
| `src/lib/rpg/catalog.ts` 的三職業、六專精、六平面升級 | v3 移除；legacy validator 保留舊合法值、能力公式與升級成本，供辨識及精確退款。 |
| `test/rpg/routeMatrix.ts` 及 `scripts/verifyRpgV2.mjs` | 保留 v2 歷史證據；v3 用實際引擎狀態計算決策，不能沿用人工決策數。 |
| `src/lib/rpg/contentValidation.ts` 的數量與事件匯流規則 | v3 替換為地圖、唯一身分及認證限制；環防刷不得先刪，須同時移交基線與一次性記錄。 |
| `scripts/exportRpgStory.mjs`、`docs/rpg-story.md`、`rpg:text` | runtime 換成 v3 時同步退役或改為規則輸出，不留下空稿命令。 |
| `src/pages/RpgGame.jsx` 的 v2 畫面與原有 mounted tests | 垂直切片規則成立後更新；保留讀寫失敗與明示轉換的恢復保護。 |
| `expand-nightmare-fortress-expeditions` | 本 change 通過後才歸檔或撤回，本輪不處理。 |

純 TS 引擎／React／StoragePort 邊界、RPG 專用 key、其他遊戲和離線安裝前提維持不變。

